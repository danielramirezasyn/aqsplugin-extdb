# aqsplugin-extdb
Stateless Docker plugin for ApiQuickServe that enables connectivity to external databases (SQL Server, PostgreSQL, MySQL). Credentials and queries are passed per-request — no configuration stored.
